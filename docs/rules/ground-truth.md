# Ground Truth

`PROJECT_SOURCE_OF_TRUTH.md` is the product, architecture, and data model source of truth.

## Current Plan Sources

- `PROJECT_SOURCE_OF_TRUTH.md` — master blueprint (vision, roles, modules, data model, stack, roadmap)

## Future Layout

As the project grows, supporting docs will be placed in:

- `docs/architecture.md` — architecture overview
- `docs/supabase-setup.md` — Supabase project setup notes
- `docs/plan/` — phase-specific implementation plans

After those exist, all implementation must read the relevant plan before starting and must update it after any approved deviation.

## Deviation Workflow

If a task would conflict with the plan:

1. Halt implementation.
2. Describe the conflict.
3. Wait for explicit approval.
4. Update `PROJECT_SOURCE_OF_TRUTH.md` if approved.
5. Continue only after the plan is current.
